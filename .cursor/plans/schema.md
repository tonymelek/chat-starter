# Schema (Cursor plan index)

**Canonical schema:** [`docs/SCHEMA.md`](../../docs/SCHEMA.md)

Do not duplicate field definitions here. Update `docs/SCHEMA.md` when the data model changes.

Quick reference:

- `users/{uid}` — user directory
- `chatRooms/{roomId}` — conversations
- `chatRooms/{roomId}/messages/{messageId}` — messages
- Storage `avatars/{uid}` — profile photos

See also: [`../../docs/SETUP.md`](../../docs/SETUP.md), [`../../docs/ROADMAP.md`](../../docs/ROADMAP.md)
